# Marathon pacing and finish-time distributions

This repository reproduces two data visualizations based on publicly available
marathon result data.

## Reproducibility
All figures are generated from scratch using the included Excel files.

## How to run
In R:

```r
source("analysis.R")


---

## STEP 7 — Test locally (DO NOT SKIP)

From inside the `marathon-viz/` folder:

1. Open R / RStudio
2. Set working directory to `marathon-viz`
3. Run:

```r
source("analysis.R")
